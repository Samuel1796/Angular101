export type User = {
    nameOfSong: string,
    artist: string,
    rating: number,
}

export type Movie ={
    title: string,
    genre: string,
    origin: string
}

export type Product = {
    name: string,
    price: number,
    description: string,
    category: string,
    image: string
}